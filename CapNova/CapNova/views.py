# Registration/login views were moved into the `accounts` app
# (accounts/views.py) as part of the authentication + dashboard-routing
# feature. This file is intentionally left empty; CapNova/urls.py no
# longer references it.
